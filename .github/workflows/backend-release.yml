name: Backend Release (DEPRECATED - use release-backend.yml)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated notice
        run: |
          echo "::warning::backend-release.yml is deprecated - use .github/workflows/release-backend.yml with tag v*.*.*"
          echo "To release: git tag v1.0.0 && git push origin v1.0.0"
          exit 0
